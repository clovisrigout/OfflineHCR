
data.py:
	- source code to load and format the needed data.
	- generates a data object saved in /data/ directory

model.py:
	- source code to train or test model
	- generates a model saved in /models/ directory
